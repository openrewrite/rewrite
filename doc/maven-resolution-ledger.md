# Maven resolution parity ledger

One row per divergence between the legacy rewrite-maven resolution engine and real Maven, per
`maven-resolution-plan/PHASE-0-SPEC.md` §5. `class` ∈ `ALIGN_TO_MAVEN` (default for anything
semantic) | `KEEP_REWRITE` (output shape/API/seams only — each row must justify itself against
DESIGN.md §0). Every mask in `rewrite-maven/src/test/resources/parity/masks.txt` must reference
a row here.

| id | area | current behavior | Maven behavior | class | evidence | pinning test | status |
|---|---|---|---|---|---|---|---|
| L-P0-001 | direct-dependency dedup | Direct dependencies deduplicate by groupId:artifactId only ("last declaration wins"), so two direct deps differing only by classifier collapse to the later declaration; the earlier requested `Dependency` resolves to nothing (`getResolvedDependency` → null) | Conflict key is g:a:classifier:type; both classifier variants resolve | ALIGN_TO_MAVEN | parity fixture `classifiers` snapshot | `IdentityContractsTest.classifierShadowedByGaDeduplication` (pins current behavior; Maven-identical pinning test lands with the Phase 3 fix) | OPEN |
| L-P0-002 | plugin execution merge | Parent/child executions with the same id merge goals through a `HashSet` (`ResolvedPom.mergePluginExecutions`), producing hash order — deterministic in and across JVMs (String hash is stable) but not declaration order (fixture: child `[enforce-child, display-info]` + parent `[enforce]` → `[display-info, enforce, enforce-child]`) | Goals merge preserving declaration order | ALIGN_TO_MAVEN | parity fixture `plugins-executions` snapshot (`pom.plugins[*].executions`) | determinism suite pins the current (stable) order; Maven-identical pinning test lands with the Phase 2 plugin-merge fix | OPEN |
| L-P0-003 | plugin list order after parent merge | A child plugin that merges with a parent declaration is removed and re-appended, moving it to the end of `ResolvedPom.plugins` (fixture: child declares enforcer before own-plugin, effective order is `[own-plugin, enforcer]`) | Effective plugin order keeps inherited/declared ordering (enforcer first) | ALIGN_TO_MAVEN | parity fixture `plugins-executions` snapshot (`pom.plugins` array order) | determinism suite pins the current (stable) order; Maven-identical pinning test lands with the Phase 2 plugin-merge fix | OPEN |

| L-P0-004 | failure handling / complete model | An unresolvable direct dependency in ANY single scope (jetty-server-11.0.19: test-scoped `org.eclipse.jetty.tests:jetty-http-tools` genuinely 404s on Central) fails the entire resolution — no `MavenResolutionResult` marker at all. NOTE (Jon): always resolving Compile/Runtime AND Test/Provided is deliberate and correct — the complete model is the contract; only the failure handling is wrong | Effective pom builds fine; compile/runtime classpaths resolve; only test-classpath resolution fails with artifact-not-found | ALIGN_TO_MAVEN (failure semantics) + KEEP_REWRITE (complete-model output shape): marker present, resolvable scopes fully populated, failing scope's error surfaced via aggregated `MavenDownloadingExceptions` → warn + `ParseExceptionResult` | corpus entry `jetty-server-11.0.19` + its ground truth | `PartialFailureCompleteModelTest` (marker survives; resolvable scopes populated; error surfaced). Fix: `MavenDownloadingExceptions.partialResult` carries the model, `MavenParser` attaches it alongside `ParseExceptionResult`. Applies to transitive failures too | **FIXED** (pre-engine, 2026-07-08) |
| L-P0-005 | `datedSnapshotVersion` on releases | Downloaded release poms get `datedSnapshotVersion == version` (plain version duplicated); project poms get null. Internally inconsistent, wire-visible | n/a (rewrite-only field; contract says the dated form is for timestamped snapshots) | TBD — Phase 3 decides reproduce-vs-fix when `DependencyGraphMapper` sets the field | slice A snapshots (`"dated": "1.1"`) + slice B corpus snapshots | to be added with the Phase 3 decision | OPEN |
| L-P0-006 | HTML-index metadata derivation | When `maven-metadata.xml` 404s and no explicit version is requested, the downloader scrapes the repository's HTML directory index (Nexus-style) for version dirs and derives metadata; a non-404 4xx on the scrape disables derivation on that repository for the rest of the run (`MavenPomDownloader.deriveMetadata`) | No equivalent — Maven fails metadata resolution when the file is absent | KEEP_REWRITE — transport-layer augmentation: only produces an answer where Maven produces an error, never a different answer where Maven has one (decided 2026-07-08) | synthetic tests: 3-request happy path (metadata 404 → index GET → derived-version pom GET), self-disabling proven by the second artifact's index never being requested | `HtmlIndexMetadataTest` (both tests pin the kept behavior) | CLOSED (KEEP) |
| L-P0-007 | unresolved `${...}` credentials on the POM path | Settings-server credentials with unresolvable placeholders are sent verbatim as basic auth on the first pom/metadata request (leaking the literal placeholder to the server); resolution recovers only through the authenticated→anonymous 4xx retry. `MavenArtifactDownloader` (jar path) instead skips `${`-containing credentials up front — the two downloaders disagree | Maven effectively does not authenticate with unresolvable credentials (verify exact transport behavior during Phase 3) | ALIGN_TO_MAVEN | synthetic auth server: 2 requests observed — first with `Authorization: Basic base64("${env...}:${env...}")`, second anonymous | `AuthFallbackTest.unresolvedPlaceholderCredentialsFallBackToAnonymous` (pins current; Maven-identical pinning test lands with the Phase 3 fix) | OPEN |
| L-P2-B2-001 | super-POM plugin/repository injection | Legacy `ResolvedPom` carries only declared/inherited `<pluginManagement>`/`<repositories>`/`<pluginRepositories>` — rewrite never merges Maven's super-POM — so these lists omit the super-POM's default managed plugins (antrun/assembly/dependency/release), the `central` repository, and the `central` pluginRepository | Maven's effective model always injects the super-POM: 4 default managed plugins + `central` repository + `central` pluginRepository | KEEP_REWRITE (output shape: the projected pom carries only real declared/inherited models; whether Central participates in resolution is an `addCentralRepository`/repository-universe decision made by the pipeline, not the effective model's super-POM) | B2 `EffectivePomMapper` keeps only entries whose `InputLocation` modelId is the root or a fetched pom (the super-POM is never fetched); with this filter every fixture's `repositories`/`pluginRepositories`/`pluginManagement` match legacy | `EngineResolvedPomComparisonTest` (all 11 fixtures, `$.pom` section) | CLOSED (KEEP) |

| L-P2-B2-002 | active-profile dependency ordering | Legacy contributes active-profile `<dependencies>` in REVERSED profile-declaration order (RawPom stores profiles reversed "into precedence order left to right", a1 §1.3), so with active profiles `jdk-any` (declared first) and `explicit` (declared last) the requested/resolved order is `explicit-dep, jdk-dep` | Maven's `DefaultProfileInjector` appends active-profile dependencies in profile DECLARATION order → `jdk-dep, explicit-dep` | ALIGN_TO_MAVEN (engine follows Maven; legacy is the outlier) | `EngineDependencyGraphComparisonTest` profile-activation fixture (`$.scopes.*` gav + requestedRef swap across all four scopes; same set, reordered) | the differential's declared flip pins it; Maven-identical pinning test lands with the Phase-5 flip | OPEN |
| L-P2-C-001 | plugin management merge/order/interpolation | Legacy `pluginManagement` keeps raw (uninterpolated) plugin coordinates (`${version.plugin.x}` stays literal) and rewrite's own merge/order (parent-merged entries re-append, goals via `HashSet`) | Maven's effective `pluginManagement`: interpolated coordinates, declaration-order-preserving merge — the same class as the `plugins` flip (L-P0-002/003) | ALIGN_TO_MAVEN | `MavenParserTest` in SHADOW mode (`$.pom.pluginManagement` re-orders + interpolates) | shadow mask `$.pom.pluginManagement`; Maven-identical pinning test lands with the Phase-3 plugin-list fix | OPEN |
| L-P2-C-002 | subprojects dropped on re-resolution | Legacy `ResolvedPom.resolve` rebuilds the effective pom from an `emptyList()` subprojects scaffold and returns it whenever any field changed, so the resolved pom's `subprojects` is empty even for a `<modules>` aggregator; the engine preserves `requested.getSubprojects()` | Maven's effective model retains the modules | KEEP_REWRITE (output shape) | `MavenParserTest` in SHADOW mode (`$.pom.subprojects`) | shadow mask `$.pom.subprojects` | OPEN |
| L-P2-C-003 | missing `<modelVersion>` tolerance | Rewrite's `RawPom` parses a pom with no explicit `<modelVersion>` (recipe tests routinely omit it), defaulting the model; Maven's `DefaultModelBuilder` rejects it (`'modelVersion' is missing`) even at `VALIDATION_LEVEL_MINIMAL` | Real Maven also rejects it — but rewrite's lenient snippet parsing is a deliberate feature | KEEP_REWRITE (the engine injects `<modelVersion>4.0.0</modelVersion>` when absent, mirroring `RawPom`, so it reads the same lenient model rewrite's parser does) | `MavenParserTest` in SHADOW mode: without the default, 84 tests produced legacy-resolved/engine-threw outcome mismatches | `MavenEngineResolution.ensureModelVersion`; covered by every `MavenParserTest` pom lacking a `<modelVersion>` | CLOSED (KEEP) |
| L-P2-C-004 | embedder repos in the projected repositories | Legacy prepends the embedder-injected `local` (`~/.m2`) and `central` repositories to `ResolvedPom.repositories`, shifting the array; the engine projects only declared/inherited repositories (Maven's effective model, super-POM filtered) | Maven's effective model lists only declared/inherited repositories; repo-universe membership is a pipeline decision (`addLocal`/`addCentral`), not the effective model | KEEP_REWRITE (extends L-P2-B2-001) | `MavenParserTest` in SHADOW mode (every `$.pom.repositories` diff was local/central inclusion + the resulting index shift; no genuine declared-repo divergence) | shadow mask `$.pom.repositories` | OPEN |

| L-P2-C-005 | `pluginRepositories` dropped by legacy resolve | `ResolvedPom.resolve` never carries `<pluginRepositories>` into the resolved model (they survive only on the unchanged-identity path, a1 §2.6); recipes see an empty list after any real resolve | The effective model always carries declared + inherited plugin repositories | ALIGN_TO_MAVEN | shadow diff `$.pom.pluginRepositories` on `SnapshotCoverageTest.pluginRepositoriesCaptured` (legacy=missing, engine=declared repo) | masked in shadow (`masks.txt`) until the Phase-2 flip; Maven-identical pinning test lands with the flip | OPEN |

| L-P2-D-001 | self-parent (parent GA == project GA) | Legacy tolerates a `<parent>` whose groupId:artifactId equals the project's and resolves the effective pom | Maven's ModelValidator rejects it: `'parent.artifactId' must be changed, the parent element cannot have the same groupId:artifactId as the project` | ALIGN_TO_MAVEN | `MavenParserTest.selfRecursiveParent` in SHADOW (legacy resolves, engine throws `MavenParsingException`) | outcome mask `outcome:self-parent`; the tolerance test inverts at the Phase-5 flip (captured failure shape) | OPEN |
| L-P2-D-002 | recursive `${...}` expression cycle | Legacy tolerates a self/mutually-referential property expression (e.g. `<revision>${revision}</revision>`), leaving the placeholder literal | Maven's interpolator raises a FATAL `Detected the following recursive expression cycle` and fails model building | ALIGN_TO_MAVEN | `MavenParserTest.selfReferencingPropertyDoesNotStackOverflow` in SHADOW | outcome mask `outcome:expression-cycle`; inverts at Phase-5 | OPEN |
| L-P2-D-003 | non-`pom` parent packaging | Legacy resolves a project whose `<parent>` pom declares (or defaults to) `jar` packaging | Maven requires a parent POM to be `packaging=pom`: `Invalid packaging for parent POM ... must be "pom" but is "jar"` | ALIGN_TO_MAVEN | `MavenParserTest.{nestedParentWithDownloadedParent,repositoryWithPropertyFromParent,differentRangeVersionInParent,managedDependenciesInParentInfluenceTransitives}` in SHADOW | outcome mask `outcome:parent-packaging`; inverts at Phase-5 (test fixtures gain `<packaging>pom</packaging>` or rewrite keeps the leniency as an announced feature) | OPEN |
| L-P2-D-004 | non-`pom` aggregator packaging | Legacy resolves an aggregator (`<modules>`) pom whose packaging is `jar` | Maven requires `packaging=pom` for aggregators: `'packaging' with value 'jar' is invalid. Aggregator projects require 'pom' as packaging` | ALIGN_TO_MAVEN | `MavenParserTest.{circularImportDependency,childDependencyDefinitionShouldTakePrecedence}` in SHADOW | outcome mask `outcome:aggregator-packaging`; inverts at Phase-5 | OPEN |
| L-P2-D-005 | genuinely missing dependency version | Legacy builds the effective pom for a `<dependency>` with no version and no managing entry, deferring failure to artifact download (a `MavenDownloadingException`) | Maven's effective-model validator rejects it: `'dependencies.dependency.version' for ... is missing` | ALIGN_TO_MAVEN | `MavenParserTest.invalidDirect` in SHADOW (legacy builds the model then throws at download; engine throws at model build) | outcome mask `outcome:missing-dependency-version`; the surfaced error becomes model-side at Phase-5 | OPEN |
| L-P2-D-006 | dependencyManagement duplicate-key collapse | Legacy's effective DM keeps one `ResolvedManagedDependency` per `g:a:classifier:type` (first-wins, a4 #5402); Maven's effective model keeps every raw DM entry (its `ModelNormalizer.mergeDuplicates` dedups only `<dependencies>`, never `<dependencyManagement>` — verified in 3.9.x source) | n/a — output-shape projection | KEEP_REWRITE (the mapper dedups the projected DM by `g:a:classifier:type` first-wins to match legacy's frozen-tree contract; `getManagedDependency` looks up by the same 4-tuple, so duplicates carry no information) | `MavenParserTest.DependencyManagement.allDependencyManagementEntryVariants_allDependencyVariants` in SHADOW (8 raw entries → 5 distinct) | `EffectivePomMapperTest.dependencyManagementDedupedByGroupArtifactClassifierType` (mapper) + the shadow suite | CLOSED (KEEP) |
| L-P2-D-007 | `${...}` in the projected gav | Legacy interpolates the requested gav's coordinates once properties are merged (CI-friendly `${revision}`, `${project.version}`, groupId/artifactId-as-property); an early engine mapper left them literal | n/a — output-shape projection | KEEP_REWRITE (the mapper interpolates the projected gav through `ResolvedPom.getValue` — same properties, same result as legacy) | `MavenParserTest.{propertyFromMavenConfig,circularMavenProperty,multipleCiFriendlyVersionPlaceholders,groupIdAndArtifactIdAsProperties,...}` in SHADOW (`$.pom.gav`) | shadow suite (11 gav tests) | CLOSED (KEEP) |
| L-P2-E-002 | metaversion in `<dependencyManagement>` | Legacy resolves a `latest`/`release` metaversion declared on a managed dependency to a concrete version (via metadata — nondeterministic, e.g. `com.google.guava:guava:latest` → `33.6.0-jre`) | Maven's effective model keeps the metaversion literal in the managed entry (concrete resolution is deferred to artifact selection) | ALIGN_TO_MAVEN (engine is Maven-correct; legacy is the outlier) | `AddDependencyTest.doesNotClobberManagedLatestWithExplicitVersion`, `UpgradeDependencyVersionTest.doesNotClobberManagedLatestWithSpecificVersion` in SHADOW (`$.pom.dependencyManagement[*].version` `"33.6.0-jre" != "latest"`) | value-conditioned mask `metaversion:$.pom.dependencyManagement` (suppresses only a legacy-resolves/engine-keeps-literal flip); inverts at Phase 5 | OPEN |
| L-P2-E-003 | profile-activated managed entries in a transitively imported BOM | Legacy's effective `<dependencyManagement>` for `spring-boot-dependencies:3.2.4` is MISSING four `org.infinispan:infinispan-marshaller-{kryo,kryo-bundle,protostuff,protostuff-bundle}:14.0.27.Final` entries — legacy's env-var-based profile activation does not activate infinispan-bom's `community` profile (`<property><name>release-mode</name><value>!downstream</value></property>`, active when `release-mode != downstream`, i.e. when unset) | The engine's effective model INCLUDES those four entries at the correct pinned version (`infinispan-bom:14.0.27.Final`, which spring-boot-dependencies:3.2.4 pins via `infinispan.version=14.0.27.Final`); they live in the BOM's property-negation-activated `community` profile, which Maven activates. Root-caused: NOT a nested-import-BOM version bug — both engines resolve infinispan-bom:14.0.27; the engine's DM is a strict +4 superset (1466 vs legacy 1462), all four verified present in 14.0.27.Final. The original "engine resolves an OLDER version" characterization was inverted | ALIGN_TO_MAVEN (engine is Maven-correct; legacy's profile activation reads `System.getenv` not properties — a1 §2.3 / DESIGN §9 seed) | `ChangeParentPomTest$RetainVersions.{bringsDownRemovedManagedVersion,bringsDownRemovedProperty,preservesOwnDefinedProperty,dependencyWithExplicitVersionRemovedFromDepMgmt}` + `UpdateMavenProjectPropertyJavaVersionTest.doNotCrashOnImplicitVersion` in SHADOW (engine DM = legacy DM + 4 infinispan entries); `NestedImportBomTest.profileNegationActivatedImportsAreIncluded` pins the engine's Maven-correct behavior hermetically | mask `dm-superset:$.pom.dependencyManagement` (directional — suppresses only engine-added managed entries legacy dropped, never a value divergence or an engine drop; the DM diff aligns by gact so the superset is 4 discrete entries, not a positional shift); inverts at the Phase-5 flip | OPEN |
| L-P2-E-001 | system-scoped dependency with an absent or non-absolute `<systemPath>` | Legacy resolves the effective pom for a `system`-scoped dependency whose `<systemPath>` is absent or not an absolute path (e.g. `${project.basedir}/lib/...`), deferring any failure | Maven's effective-model validator rejects it at model build: `'dependencies.dependency.systemPath' for <g:a> is missing` / `... must specify an absolute path but is <path>` | ALIGN_TO_MAVEN | `AddDependencyTest.handlesSystemScopeToAllowableScopeChange` + `NoSystemScopeDependenciesTest.{removesSystemScopeAndSystemPath,removesSystemScopeWhenVersionIsProperty,removesSystemScopeInDependencyManagement,doesNotRemoveSystemScopeWhenNotInRepo}` in SHADOW (legacy resolves, engine throws `MavenParsingException`) | outcome mask `outcome:system-scope-missing-path`; the tolerance tests invert at the Phase-5 flip | OPEN |
| L-P2-D-008 | parser/config-injected property precedence | Legacy bakes `MavenParser.builder().property(...)` values into every project pom's properties at parse time (`putAll`), so they override a POM-declared property of the same name; an early engine mapper overlaid them with `putIfAbsent` | n/a — matches legacy | KEEP_REWRITE (the mapper overlays injected properties with `putAll` override) | `MavenParserTest.projectVersionPropertyOverriddenByBuilderProperty` in SHADOW (`project.version` 9.9.9 vs declared 1.2.3) | shadow suite | CLOSED (KEEP) |

Phase-3 slice C wired dependency resolution (`resolveDependencies`) through the facade, so the SHADOW oracle now
diffs the full `pom`+`scopes`+`errors` snapshot. Local-repository URI spellings normalize to `<localrepo>` (legacy's
`~/.m2/repository` vs the engine's `file:///…/.m2/repository/` are the same repository — `SnapshotNormalizer`, not a mask).
The rows below are the dependency-graph (`$.scopes`/`$.errors`) divergence classes surfaced by the full-suite SHADOW
census. Following the L-P2-E-003 precedent, engine DROPs and value divergences are **left red and unmasked** rather than
mask-hidden (a mask that could hide a drop violates the parity discipline); they are the next slice's mapper/collector work.

| id | area | current behavior | Maven / engine behavior | class | evidence | pinning test | status |
|---|---|---|---|---|---|---|---|
| L-P3-C-001 | transitive `requested` version threading | Legacy threads the declaring pom's *raw declared* version onto a transitive `ResolvedDependency.requested` — `null` when the coordinate was declared without a version (its version came from management) | The mapper threads the declaring pom's raw declared version via `matchDeclared`; a `${project.groupId}` placeholder in the declarer's raw coordinate is interpolated before matching, and a reactor-member declarer is now recorded in the pom cache (see L-P3-D-001) so its raw coordinate is readable | KEEP_REWRITE (mapper mirrors legacy's raw-declared threading; `requested` records what was declared) | `MavenParserTest.{runtimeClasspathOnly,managedDependencyInTransitiveAndPom,groupIdAndArtifactIdAsProperties}` in SHADOW | `DependencyGraphMapper.{transitiveRequested,matchDeclared,matchRequested}` (`${...}` interpolation + reactor recording) | **FIXED (slice D)** |
| L-P3-C-002 | `effectiveExclusions` completeness | The engine's `ExclusionAttributor` reported FEWER pruned coordinates than legacy (wildcard expansion, DM-managed exclusion, reactor-crossing) and, once seeded, mis-attributed them to the root-most declarer | Legacy records every coordinate its inline `includedByMap` walk pruned, attributed to the shallowest ancestor in the unbroken *requested*-exclusion chain | ALIGN_TO_MAVEN (attributor completeness + attribution target) | `MavenParserTest.{wildcardExclusion,exclusionsAffectTransitiveDependencies,missingDependencyIsExcluded}`, `RemoveRedundantDependencyVersionsTest.preservesExplicitVersionIfNotRequested` in SHADOW | `ExclusionAttributor` (rewritten: effective parent-merged declared deps enumerated from `EngineCollectOutcome.declaredDependencies`; per-node effective vs requested exclusions from the aether node; path-based walk-up attribution) | **FIXED (slice D)** |
| L-P3-C-003 | reactor-served transitive attribution | A dependency reached transitively THROUGH a reactor member — or served WARM from the shared DataPool on a later reactor module's collect — was absent from `servedBy`, so `repository`/`datedSnapshotVersion`/`licenses` were null | Legacy attributes repository/dated/licenses from the download | ALIGN_TO_MAVEN | `MavenParserTest.managedDependencyInTransitiveAndPom` in SHADOW (`$.scopes.*[1].{dated,licenses,repo}`) | `servedBy`/`declaredDependencies` made **cumulative across collects** on `EngineDependencyCollector` (a warm read skips `EngineDescriptorReader` entirely, so per-collect attribution dropped it); `EngineDescriptorReader.recordReactorServed` records reactor members | **FIXED (slice D)** |
| L-P3-C-004 | transitive-closure graph divergence | A subset of poms resolve a *different* transitive closure between engine and legacy (different `gav`/`depth`/`children`/scope-membership at a scope index) | — | split by ground truth (below) | `MavenParserTest.{repositoryWithPropertyPlaceholder,invalidTransitives}` + `RemoveRedundantDependencyVersionsTest.dontOverrideDependencyConfigurations` in SHADOW | see L-P3-D-001 (scope under-population, FIXED), L-P3-A-001 (compile-scope widening, FIXED), and L-P3-D-003 (residual mediation) | ARBITRATED (Phase-4 slice A) |
| L-P3-A-001 | compile-scope under-population of a widest-scope conflict winner | The mapper dropped a coordinate from the **compile** classpath when its raw declared scope in the nearest containing pom was `runtime` but another (deeper) path declares it `compile` — `DependencyGraphMapper.childScope` read the raw declared scope (`runtime`) and excluded it, though Maven keeps it `compile`. Canonical case: `io.grpc:grpc-core:1.60.1` declares `error_prone_annotations:2.20.0` at `runtime` (direct, depth 1) while `grpc-api` declares it `compile` (depth 2); the engine dropped it from compile, `mvn dependency:tree` and legacy keep it (`error_prone_annotations:2.20.0:compile`, "scope not updated to compile") | Maven selects a coordinate's effective scope as the widest across every path to its winning version; aether records that on the winner edge. `childScope` now trusts aether's edge scope when it is **wider** than the raw-declared scope; when aether instead PROMOTED it narrower (a compile child of a test/provided parent), the declared scope is wider and wins, preserving L-P3-D-001 | ALIGN_TO_MAVEN (Maven == legacy → engine bug) | ground truth: `mvn 3.9.14 dependency:tree` on `grpc-core:1.60.1` (compile) lists `error_prone_annotations`; dual-engine probe: `grpc-core` compile 4→4 SAME SET after the fix | `CompileScopeWideningTest.wideningToCompileMatchesMaven` (MAVEN mode, real grpc-core; fails pre-fix, passes post-fix) — a hermetic fixture proved elusive because reproducing the aether conflict+scope relink needs `grpc-core`'s exact three-version topology | **FIXED (slice A)** |
| L-P3-D-001 | Test/Provided scope under-population | The engine's Test/Provided scope lists omitted the compile/runtime transitives of a test/provided-scoped *direct* dependency: aether's `JavaScopeDeriver` promotes a compile child of a test parent to `test`, and the mapper's `isInClasspathOf` then excluded it | Legacy propagates every root as Compile and uses the child's *declared* (non-derived) scope; real `mvn dependency:tree` for a test-scoped `junit-jupiter-api` lists apiguardian/opentest4j/platform-commons as `:test` (they ARE the test closure) | ALIGN_TO_MAVEN (Maven agrees with legacy — engine bug) | ground truth: `mvn dependency:tree` (captured); `MavenParserTest.{parse,diamondProblem,inheritScopeFromDependencyManagement}` in SHADOW | `DependencyGraphMapper.childScope` replicates `ResolvedPom.getDependencyScope` (declared/containing-managed scope + root-DM no-promote override) instead of reading aether's derived scope | **FIXED (slice D)** |
| L-P3-D-002 | local-cache shadows remote in `repository` attribution | Legacy's file-repo convention (a2 §1): `~/.m2` (any `file://` repo) only *serves* a jar-packaged artifact when a non-empty sibling jar exists — a bare pom never rewrites attribution (the remote origin is recorded); with the jar present, legacy serves it and attributes the local repo. The engine's `CacheBridge` attributed whichever repo served bytes, so a pom-only `~/.m2` entry flipped attribution to `<localrepo>` | Legacy-identical after the gate; the SHADOW census no longer depends on `~/.m2` contents | ALIGN (to legacy's file-repo jar gate; pom/other packaging exempt exactly as `MavenPomDownloader`) | machine-independence proven: full shadow `MavenParserTest` on a populated `~/.m2` shows zero `$.scopes.*[i].repo` attribution diffs | `CacheBridge.resolvePom` file-repo gate (`jarPresentForJarPackaging`) | **FIXED (slice E)** |
| L-P3-D-004 | BOM-imported managed-dependency `requestedRef` threading | Root-caused (slice E): NOT a BomGavAttributor gap. The divergence only fires on a recipe's RE-resolution — `UpdateMavenModel` swaps a re-mapped `requested` (fresh `Imported` instances) onto the old `ResolvedPom`, and legacy's `ResolvedPom.resolve` no-change gate returns the CALLER's instance, whose DM still threads the pre-edit declarations (`dmRequestedRef` → `inherited:g:a` against the new requested); the engine's graph path rebuilt fresh and threaded the new instances (`bom[0]`) | Legacy's no-change contract is the pinned API behavior; the engine now honors it | KEEP_REWRITE (replicate the no-change identity contract on the engine graph path) | `RemoveDuplicateDependenciesTest.removeDuplicatedDependencyWithImportScope`, `UpgradeDependencyVersionTest.{upgradeAllManagedDependenciesToPatchReleases,…}` in SHADOW | `MavenEngineResolution.attemptEngineGraph` threads against the caller's `ResolvedPom` whenever the engine's threading inputs (requested deps + managed coordinates, gact-aligned, repository-spelling-insensitive) are value-equal (`threadingEqual`) | **FIXED (slice E)** |
| L-P3-D-003 | transitive jar POM 404 tolerate-vs-fail + residual mediation | (a) FIXED (slice E): the collector now fails a jar/ejb/empty-type POM-404 at ANY depth when the node is a resolved-graph member (conflict-winner, non-optional); a pom-typed entry, optional dep, or conflict-loser stays tolerated; the failure's `root` is its depth-1 ancestor so only that ancestor's scopes are failed (legacy a1 §3.9 + real-Maven arbitration, phase3-d). (b) A small residue of genuine version-range/mediation divergences remains OPEN | (a) legacy+Maven fail a real jar POM 404 | (a) ALIGN_TO_MAVEN (Maven == legacy); (b) per-case ground truth | `MavenParserTest.{invalidTransitives,propertyFromMavenConfig}` (coherence case now matches); pinned by `EngineDependencyCollectorTest.missingJarTransitiveFails` (updated from `missingTransitiveTolerated`, which pinned the pre-arbitration tolerance) | `EngineDependencyCollector.outcome`/`walk` (winner/optional/type-aware any-depth split) | (a) **FIXED (slice E)**; (b) OPEN (`gav-mediation` residue) |
| L-P3-C-005 | `$.errors` message shape | A resolution failure's `MavenDownloadingException` message text differs between legacy (`"Unable to download POM: <gav>."`) and the engine collector; the engine now embeds the GAV (`EngineDependencyCollector`, mirrors the downloader) so consumers grepping the message keep working, but the normalized `$.errors` text can still differ | — | ALIGN_TO_MAVEN (error text) | `MavenParserTest.mirrorsAndAuth` in SHADOW (`$.errors[0]` message text) | `PartialFailureCompleteModelTest` (both scopes; MAVEN + LEGACY green) proves the GAV is in the message | OPEN (message-text normalization residual) |
| L-P3-C-006 | resolution event stream | The 13 `ResolutionEventListener` events are emitted at semantically-equivalent but re-ordered points on the engine path (parallel prefetch, mapper-time emission) | Per DESIGN §5.6 the event-ordering contract changes at cutover; events are exactly-once by construction | n/a — contract change | events are **excluded** from the SHADOW diff (`MavenEngineResolution.assertShadowDependencyParity` filters to `pom`/`scopes`/`errors`); no mask | — | NOTE (documented, excluded) |
| L-P0-001 (type-filter) | non-jar dependency types resolve | Legacy applies a type filter in `doResolveDependencies` that skips non-`jar/ejb/pom/zip/bom/tgz` types; the mapper applies NO type filter, so `test-jar`/`war`/etc. resolve (Maven-correct) | Maven resolves all declared types | ALIGN_TO_MAVEN (the type filter is the removed parity bug, same family as L-P0-001 classifier dedup) | `RemoveDuplicateDependenciesTest.keepDependencyWithType` in SHADOW (engine resolves the `war`-typed dep legacy skips) | mapper as built (no type filter) | OPEN (no dedicated pin yet) |
| L-P3-E-001 | duplicate direct declarations in `requestedDependencies` | Legacy's `mergeRequestedDependencies` copies the FIRST non-empty contribution whole — a pom's own duplicate g:a declarations are BOTH retained (resolution is last-declaration-wins and threads the resolved dep to the LAST instance, `root[1]`); the engine's mapper deduped by GA within a pom, breaking the `requestedRef` index | n/a — output-shape/identity projection | KEEP_REWRITE (mapper mirrors legacy's merge exactly) | `MavenParserTest$DependencyManagement.*` (duplicate jackson-core declarations), `RemoveDuplicateDependenciesTest.{removeSingleDuplicate,…}` in SHADOW (`root[N] != root[0]`) | `EffectivePomMapper.mergeRequested` (first-contribution-whole + ancestor adds only missing g:a) | **FIXED (slice E)** |
| L-P3-E-002 | transitive `requested` from a parent-inherited declaration | Legacy's `getRequestedDependencies()` is parent-merged, so a transitive declared only in the declaring pom's PARENT (e.g. `jackson-core` inherited into `jackson-dataformat-toml` from `jackson-dataformats-text`) threads that inherited raw declaration (version null when managed); the engine's `matchDeclared` scanned only the raw pom's own `<dependencies>`, missed it, and fell back to aether's managed version. Placeholder versions also interpolate through the lineage's raw property merge exactly as `ResolvedPom.getValue` (unresolvable stays literal) | n/a — matches legacy | KEEP_REWRITE (mapper mirrors legacy's parent-merged threading) | `UpgradeDependencyVersionTest.upgradeVersionDefinedVia{Implicit,Explicit}PropertyInRemoteParent*`, `MavenParserTest.{differentRangeVersionInParent,jaxbRuntime}` in SHADOW (`val:g:a:null != val:g:a:<managed>`) | `DependencyGraphMapper.{Index.lineageFor,matchDeclared,transitiveRequested,interpolate/lineageProperty}` (child-wins ancestor walk + getValue-mirroring interpolation) | **FIXED (slice E)** |
| L-P3-E-003 | shaded super-POM default plugin groupId | A plugin declared without a groupId inherits Maven's default through the shaded super-POM resource, surfacing `org.openrewrite.maven.engine.shaded.org.apache.maven.plugins` in the projected coordinate (phase2-b2 deviation #5, flagged then) | The real `org.apache.maven.plugins` | ALIGN (strip the relocation prefix) | `AddAnnotationProcessorTest.*`, `ChangePluginGroupIdAndArtifactIdTest.*` in SHADOW (`$.pom.plugins[*].ga`) | `EffectivePomMapper.unshade` | **FIXED (slice E)** |
| L-P3-E-004 | metaversion (`latest`/`release`) as a direct dependency version | The collector now resolves a `RELEASE`/`LATEST` dependency version through merged metadata before seeding the collect (`EngineDependencyCollector.collectRequest` → `CacheBridge.resolveHighestMatchingVersion`, the same metadata path parents/BOMs already use — `RELEASE`→`<release>`, `LATEST`→`<latest>`); the seeded node carries the concrete version so the descriptor read no longer requests `artifact-LATEST.pom` | Maven resolves metaversions via metadata at collection | ALIGN (engine now matches legacy+Maven; the effective model keeps the metaversion literal, exactly as Maven defers concrete selection) | `UpgradeDependencyVersionTest.doesNotClobber{Direct,Managed}LatestWithSpecificVersion`, `AddDependencyTest.doesNotClobber*` in SHADOW (now green) | `MetaversionResolutionTest.{releaseMetaversionResolvesToReleaseVersion,latestMetaversionResolvesToLatestVersion}` (hermetic, MAVEN mode, mock `maven-metadata.xml` distinguishing `<release>`/`<latest>`) | **FIXED (slice F)** |
| L-P3-E-005 | duplicate g:a declarations with different scope/classifier | Maven's `ModelNormalizer.mergeDuplicates` collapses duplicate `<dependencies>` by g:a:type:classifier (last wins) BEFORE collection, so the engine's per-scope lists carry one declaration; legacy resolves each scope's last MATCHING declaration (a compile+test duplicate populates both) — the L-P0-001 dedup family at scope granularity | Maven collapses the duplicates (engine is Maven-correct) | ALIGN_TO_MAVEN (legacy is the outlier, same family as L-P0-001) | ground truth (slice F, `mvn 3.9.14 dependency:tree`): a compile+test guava duplicate → Maven WARNS "must be unique" and keeps the LAST (test) — engine-identical; a jar+war (different type) and a plain+classifier duplicate → Maven keeps BOTH — engine-identical. `RemoveDuplicateDependenciesTest.{keepDependencyWithDifferentScope,keepDependencyWithClassifier,keepDependencyWithType}`, `UpgradeDependencyVersionTest.retainReleaseOrLatestIfUsed`, `IdentityContractsTest` classifiers fixture in SHADOW | LEGACY pins (green today, flip to Maven-identical at Phase 5): `MavenParserTest.lastListedDependencyIsUsedForScope` (per-scope last-wins), `IdentityContractsTest.classifierShadowedByGaDeduplication` (classifier), `RemoveDuplicateDependenciesTest.keepDependencyWithType` (type filter) | OPEN (flips at Phase 5 with L-P0-001) |
| L-P3-E-006 | duplicate g:a declarations *inside a transitive descriptor* mis-scoped | The mapper's `DependencyGraphMapper.childScope`/`matchDeclared` reads a transitive's DECLARED scope from the *first* matching raw declaration, but Maven's `ModelNormalizer` keeps the LAST (e.g. `org.eclipse.persistence.moxy:4.0.0` declares `persistence.core` compile-then-test → effective scope is test, excluded from compile). The engine threads the first (compile) and over-includes the coordinate in the compile classpath | Maven+legacy exclude it (last-wins = test) | ALIGN_TO_MAVEN (Maven == legacy → engine bug). A blunt `matchDeclared` last-wins fix regressed 81 tests (real-world poms where legacy's scope derivation coincides with first-wins, e.g. petclinic/logback), so a targeted fix (dedup only genuine intra-pom g:a:type:classifier duplicates, at scope-lookup only) is deferred | `MavenParserTest.repositoryWithPropertyPlaceholder` in SHADOW (`$.scopes.Compile` gains `org.eclipse.persistence:org.eclipse.persistence.core:4.0.0`) | (none — OPEN, left red+unmasked per drop-safety) | OPEN (mediation residue) |

Phase-3 slice G closed the corpus gate's four NEW divergence classes (corpus-shadow-results.md). Each was
root-caused against the recorded corpus and, where it is a rewrite-output-shape or engine-input concern, fixed and
pinned; the machine-dependent profile-activation ones became deterministic via a canonical activation snapshot.

| id | area | current behavior | Maven / engine behavior | class | evidence | pinning test | status |
|---|---|---|---|---|---|---|---|
| L-P3-G-001 | `${project.parent.version}` (and the `project.parent.*` namespace) not interpolated in threaded transitive `requested` | The mapper's `interpolate`/`lineageProperty` (mirroring `ResolvedPom.getValue`) resolved plain properties and `${project.version}` but left `${project.parent.groupId/artifactId/version}` literal, so the placeholder survived into a transitive `ResolvedDependency.requested`; legacy interpolates it | Legacy interpolates the Maven built-in project-model expression to the parent's coordinate | KEEP_REWRITE (mapper mirrors legacy's interpolated threading — output/API shape; extends L-P3-C-001/L-P3-E-002) | corpus `apache-dubbo-3.2.10` (`dubbo-cluster`): 16 `requestedRef` lines `val:...dubbo-common:${project.parent.version}` → `...:3.2.10`; after the fix the corpus diff's `project.parent.version` count is **0** | `EngineDependencyGraphComparisonTest` fixture `parent-version-expr` (a transitive declared `<version>${project.parent.version}</version>`; both engines thread `2.5`) | **FIXED (slice G)** |
| L-P3-G-002 | `<os>`/`<jdk>`/`<file>`-condition profile activation injects/overrides model properties (machine-dependent) | The engine's model builder reads the live `System.getProperties()` for `<os>`/`<jdk>` activation, so it activates os/jdk profiles Maven activates but legacy (which never evaluates os/jdk/file activation) does not — injecting machine-dependent properties (`os.detected.*`, `build.platform`, `argLine.alpnAgent`, netty `tcnative-*`) and a `<file><exists>` JDK `srczip` system dep (guava). The engine is Maven-correct; legacy is the outlier | Real Maven activates `<os>`/`<jdk>`/`<file>` profiles from the session system properties + filesystem | ALIGN_TO_MAVEN (engine Maven-correct, legacy never activates — same family as L-P2-E-003). Made a reproducible **embedder input**: `MavenExecutionContextView.setActivationSystemProperties` (a resolution input like `setActiveProfiles`; default = host `System.getProperties()` = Maven parity). The corpus runner pins a canonical Linux/amd64/JDK-17 snapshot with a sources-free `java.home=/opt/canonical-jdk-17`, so `<file><exists>` JDK profiles never fire and os/jdk activation is byte-reproducible | corpus, pinned canonical: `hadoop-client-3.3.6` residual = 1 property (`build.platform`), `netty-handler` = 1 (`argLine.alpnAgent`), both deterministic; `guava-33.0.0-jre` → **CLEAN** (srczip no longer activates → matches legacy's omission) | `MavenExecutionContextView.setActivationSystemProperties`/`EngineEffectivePom.systemProperties`; corpus `CorpusResolutionRunner.CANONICAL_ACTIVATION_PROPERTIES` | **OPEN** — one knob, engine-side consumption only; flagged to Jon (recommend a directional `pom.properties` ALIGN mask + Phase-5 flip; not masked pending sign-off) |
| L-P3-G-003 | remote-repository mirror attribution | The collect resolved transitives through `requested.getEffectiveRepositories()` — the requested pom's OWN declared repositories only — so a pom that inherits a repository from its parent never had it in the request universe; bytes were attributed to canonical Central instead of the inherited mirror. Legacy's download walks the resolved pom's parent-merged repositories | Legacy attributes the repository the bytes came from post-mirror (the declared/inherited mirror) | KEEP_REWRITE (`repository` is rewrite output shape; match legacy — the repo the bytes came from). The collect now resolves through the parent-merged effective-model repositories in declaration order (`MavenEngineResolution.modelRepositories`, canonical-central dropped and re-added by the `addCentralRepository` seam so a ctx-injected `central` is not clobbered) | corpus `spark-core-2.13-3.5.0`: spark-parent declares `gcs-maven-central-mirror` before `central`; the diff collapsed **1540 → 6 lines** (repo attribution now `…storage-download.googleapis.com…` on both sides; residual is L-P3-C-004 protobuf-java drop + L-P3-G-002 os-classifier) | `EngineResolvedPomComparisonTest`/`EngineDependencyGraphComparisonTest` (all fixtures green; parent-declared-repo attribution exercised by `MetaversionResolutionTest` ctx-central not clobbered) | **FIXED (slice G)** |
| L-P3-G-004 | profile-activated `system`-scoped dependency retained in `requestedDependencies` | The engine's `requestedDependencies[0]` was `jdk:srczip:999` (scope `system`), a guava `<file><exists>${java.home}/../src.zip</exists>`-activated system dep; every `requestedRef` shifted +1 vs legacy, which never activates the file profile so omits it | Maven's effective model carries the system dep when the file profile is active (genuine Maven-semantics difference, not a projection-shape one) — engine is Maven-correct | ALIGN_TO_MAVEN (subsumed by L-P3-G-002: the file-exists activation). No mapper change — the engine's projection is Maven-correct; determinism comes from the pinned sources-free `java.home` | corpus `guava-33.0.0-jre` → **CLEAN** once the canonical `java.home` suppresses the srczip file-profile | (covered by L-P3-G-002's corpus CLEAN) | **RESOLVED via L-P3-G-002 (slice G)** |

Determinism audit note (Phase 0 slice A): resolving every slice-A fixture twice with fresh
isolated caches produced zero diffs — no nondeterminism masks were required. The suspected
plugin-goal `HashSet` ordering (study a1) is order-scrambling but stable, hence rows
L-P0-002/L-P0-003 rather than masks. DESIGN.md §9 seeds are deliberately not yet copied here;
they land with the phases that flip them.
