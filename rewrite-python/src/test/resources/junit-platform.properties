# Automatically register the Python RPC reset extension for all tests
# This prevents memory accumulation across tests by clearing cached state after each test
junit.jupiter.extensions.autodetection.enabled=true
