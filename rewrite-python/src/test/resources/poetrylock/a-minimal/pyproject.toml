[tool.poetry]
name = "a-minimal"
version = "0.1.0"
description = "minimal legacy poetry project"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
six = "1.16.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
