[tool.poetry]
name = "b-graph"
version = "0.1.0"
description = "requests transitive graph"
authors = ["Test <test@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "2.32.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
